{
  "name": "rectangle-dao",
  "version": "1.0.0",
  "description": "We change the area of our rectangle using governance",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "solhint 'contracts/**/*.sol'",
    "prepare": "husky install",
    "propose": "npx hardhat run scripts/examples/propose.js --network localhost",
    "vote": "npx hardhat run scripts/examples/vote.js --network localhost",
    "execute": "npx hardhat run scripts/examples/execute.js --network localhost"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.7.3",
    "dotenv": "^16.0.3",
    "hardhat": "^2.11.2"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@nomiclabs/hardhat-ethers": "npm:hardhat-deploy-ethers@^0.3.0-beta.13",
    "ethers": "^5.7.1",
    "hardhat-deploy": "^0.11.18",
    "husky": "^8.0.1",
    "solidity-coverage": "^0.8.2"
  }
}
